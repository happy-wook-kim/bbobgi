# 주사위 슬램 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 내려치기(슬램) 인터랙션이 있는 주사위 게임 — 확정/물리 두 모드를 토글로 제공.

**Architecture:** 순수 엔진(`dice.ts`)이 구역 격자·확정 경로·물리 적분을 제공하고, `DiceSlam.tsx`가 단일 rAF로 재생. 확정 모드는 슬램 시 동일 목표로 재계획 → 결과 불변, 물리 모드는 충격·마찰이 결과를 결정.

**Tech Stack:** 기존과 동일 (React 19 + TS + Vite, Vitest, pnpm)

**Spec:** `docs/superpowers/specs/2026-07-16-dice-slam-design.md`

## Global Constraints

- 커밋은 작업 단위마다 (Co-Authored-By 금지), 푸시는 지시 시에만
- 랜덤·점수 풀 미편입 — `randomKind.ts` 무변경
- 상수: 마찰 λ 1.4/s · 벽 패딩 0.06 · 반사 감쇠 0.8 · 정지 임계 0.03 · 구역 여유 15%

---

### Task 1: 엔진 `dice.ts` (TDD)
- [ ] dice.test.ts 작성(스펙 §6) → 실패 확인
- [ ] dice.ts 구현(스펙 §3) → 통과, `pnpm test && pnpm build`

### Task 2: `DiceSlam.tsx` + 스타일 + 통합
- [ ] 컴포넌트(스펙 §4)·CSS 작성
- [ ] types/GameStage/ChooseAnimation/SetupScreen 통합(스펙 §5)
- [ ] `pnpm test && pnpm build`

### Task 3: 검증·커밋
- [ ] Playwright: 두 모드 각각 던지기→슬램(키·탭)→정지→결과 발표, 확정 모드는 슬램 후에도 걸린 사람 불변 확인
- [ ] 작업 단위 커밋
